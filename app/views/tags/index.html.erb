<h1>All Tags</h1>

<h3> Tags:</h3>

<ul>
  <% @tag.each do |t| %>
    <li>
      <%= link_to t.name, tag_path(t) %>
    </li>
  <% end %>
</ul>

<br><%= link_to "<< Back to Articles List", articles_path %>
